import { HeaderButton } from "./HeaderButton"

export const Header = () => {
    return(
        <header>
            <h2 className="title">Túlio</h2>
            <HeaderButton/>
        </header>
    )
}